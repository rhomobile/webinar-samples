<div id="pageTitle">
		<h1>Edit <%= @person.name%></h1>
</div>

<div id="toolbar">
    <%= link_to "Back", :action => :show, :id => @person.object %>
    <%= link_to "Delete", :action => :delete, :id => @person.object %>
</div>


<div id="content">
    <form method="POST" action="<%= url_for :action => :update %>">
    	<input type="hidden" name="id" value="<%= @person.object %>"/>
            <table>
        		
                    <tr>
            			<td class="itemLabel">Name: </td>
              			<td class="itemValue"><input type="text" name="person[name]" value="<%= @person.name %>"/></td>
          		    </tr>
        		
                    <tr>
            			<td class="itemLabel">Age: </td>
              			<td class="itemValue"><input type="text" name="person[age]" value="<%= @person.age %>"/></td>
          		    </tr>
        		
                    <tr>
            			<td class="itemLabel">Location: </td>
              			<td class="itemValue"><input type="text" name="person[location]" value="<%= @person.location %>"/></td>
          		    </tr>
        		
            </table>
            <input type="submit" class="standardButton" value="Update" />
    </form>
</div>
